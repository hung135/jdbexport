QueryToCSV:
  sys-dev:
    create:
      - writePath: "./reports/{today}-{taskName}-{connection}-{qualifier}.csv"
        sql: "SELECT * FROM dbo.mockData1"
      - writePath: ./reports/{today}-{taskName}-{connection}-{qualifier}.csv
        sql: SELECT * FROM dbo.mockData1